apply from:'../module.build.gradle'

android {

    defaultConfig{
        //如果是独立模块
        if (isBuildModule.toBoolean()) {
            applicationId "com.zyh.module_web"
        }

        //统一资源前缀，规范资源引用
        resourcePrefix "web_"
    }
}

dependencies {

    api project(':lib_base')
}